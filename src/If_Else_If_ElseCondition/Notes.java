package If_Else_If_ElseCondition;
/*
             -:if:-     
If- If is used when we want to  test a condition .

Syntax- 
        if (condition)
        {
          (Statement)
        }  
         
   Note- If condition should be true.       
________________________________________________________________________________________________________________________             
             -:if else:-
 
 2. if else - It is use to execute Two statement either if statement or else statement for a single condition.
 
  Syntax :- 
           if (condition)
           {
               (Statement)
           }
           else
           {
               (Statement) 
           }
 Note--> if condition is true code will execute if statement. OR if state is false than code will execute else statement.
  ________________________________________________________________________________________________________________________
3.                  -: if else ladder :- 
if else ladder is used when we have only one if block, multiples else if blocks and at one else blocks.
 
Syntax:- 
        If(condition)
        {
           (statement)
        }
        else if (condition)
        {
           (statement)
        }
        else if (condition)
        {
           (statement)
        }
        else 
        {
          (statement)
        }


*
*/